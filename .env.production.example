# Expo CLI will load environment variables with EXPO_PUBLIC_ prefix,
# like npx expo start, npx expo export
# To specify the environment, run NODE_ENV=production npx expo start

EXPO_PUBLIC_ENV_NAME=production

# API URL Configuration:
#
# Option 1 (Recommended for Unified Deployment): Use relative path
# Frontend and backend are served from the same domain through unified nginx gateway
# EXPO_PUBLIC_API_URL=
#
# Option 2: Use full domain URL
# EXPO_PUBLIC_API_URL=https://yourdomain.com
#
# When using unified deployment with nginx-gateway, leave empty or use relative path
# The nginx gateway will route /api/, /admin/, /static/, /media/ to backend
EXPO_PUBLIC_API_URL=
